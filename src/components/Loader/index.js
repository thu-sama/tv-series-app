import React from "react";
import loader from "../../assets/loader.gif";

const Loader = props => (
	<div>
		<img src={loader} alt="Loader icon" style={{ marginTop: 20 }} />
	</div>
);

export default Loader;
